from setuptools import setup

setup(
    name='image-to-palette',
    version='1.0.0',
    install_requires=[
        "argparse",
        "Pillow",
        "matplotlib",
        "scikit-learn",
        "numpy"
    ]
)